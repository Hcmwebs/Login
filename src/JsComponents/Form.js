import Formgroup from '../JsComponents/Formgroup';

const Form = () => {
  return (
    <div className='formContainer'>
      <form action='' method='post'>
        <Formgroup />
      </form>
    </div>
  );
};

export default Form;
