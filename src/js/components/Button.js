

const Button = () => {
  const btnPrimary = 'btnPrimary';

  return (
    <>
      <button className={btnPrimary} id={btnPrimary}>
        Send
      </button>
    </>
  );
};

export default Button;
